export enum apiRoutes {
  base = '/api',
  base_auth = '/api/auth',
  register_api = '/api/auth/register',
  login_api = '/api/auth/login',
}

export enum pagesRoutes {
  base = '/',
  register = '/register',
  login = '/login',
  settings = '/settings',
}